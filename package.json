{
  "name": "murder-mystery",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "vp run -r dev",
    "build": "vp run -r build",
    "deploy": "vp run -r build && vp run -r deploy",
    "ui": "vp run --no-cache --filter=@repo/ui ui",
    "ui:memo": "vp run --no-cache --filter=memo ui",
    "ui:pdf-manager": "vp run --no-cache --filter=pdf-manager ui",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.24.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
